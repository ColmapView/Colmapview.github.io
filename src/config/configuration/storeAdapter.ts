/**
 * Store Adapter
 *
 * Re-exports the generated store adapter functions from the property registry.
 */

export {
  extractConfigurationFromStores,
  applyConfigurationToStores,
  resetToDefaults,
} from '../registry/generators/adapter';
